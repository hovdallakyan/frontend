# Backend API base URL. Leave unset in development — Vite proxies /api to the
# NestJS backend (see vite.config.ts). Set this in production builds.
# VITE_API_URL=https://your-backend.example.com/api
